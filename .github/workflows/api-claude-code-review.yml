name: API Code Quality

on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - 'apps/api/**'

jobs:
  claude-code-review:
    # if: |
    #   github.event.pull_request.user.login == 'external-contributor' ||
    #   github.event.pull_request.user.login == 'new-developer' ||
    #   github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'

    if: |
      !contains(github.event.pull_request.title, '[WIP]')

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-code-review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

          claude_args: |
            --model claude-sonnet-5
            --allowedTools "Read,Grep,Glob,Bash(git diff:*),Bash(git log:*),Bash(git show:*),mcp__github_comment__update_claude_comment"

          use_sticky_comment: true
          show_full_output: true

          # Automated review prompt (no @claude mention needed)
          prompt: |
            Post your review as a PR comment using the mcp__github_comment__update_claude_comment tool — a review left only in your final text output is lost.

            Review this pull request (Bun + Hono + TypeScript API). Score each area 1–10. Be concise — one short justification per section, max 3 actionable issues if any.

            If a previous review comment exists on this PR, use it as the baseline:
            - Mark items that have been fixed since the last push with ~~strikethrough~~ and a ✅ suffix.
            - Keep unresolved items and their scores as-is.
            - Adjust scores and add new issues introduced by the latest push.

            Scoring criteria:
            - **🏗️ Architecture**: DRY, SOLID, modularity, low coupling. Adjust ±1 for data layer (schema, transactions, indexing) or infra (cache, queues, retries) if relevant.
            - **🛡️ Security**: Input validation, auth/RBAC enforcement, no data leaks, no injection/XSS/CSRF.
            - **🧪 Testing**: ~60% coverage on critical paths and business logic. Clear and reliable tests.
            - **⚡ Performance**: Efficiency under load. Flag only real issues, not speculative.
            - **💎 Code Quality**: Readable, maintainable, names express intent. ESLint and CLAUDE.md compliant.

            DO NOT suggest:
            - JSDoc or obvious inline comments
            - Stylistic nitpicks
            - Trivial renames
            - Explicit return types when TypeScript infers correctly

            Format:

            ```md
            ## Quality Assessment

            ### 🏗️ Architecture: X/10
            [One sentence justification. Issues if any — 🔴 critical first, then 🟠 medium. Max 3.]

            ### 🛡️ Security: X/10

            ### 🧪 Testing: X/10

            ### ⚡ Performance: X/10

            ### 💎 Code Quality: X/10

            ### [✅ if overall ≥ 8, ⚠️ if < 8] Overall: X/10
            ```
